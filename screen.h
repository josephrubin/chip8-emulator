#ifndef CHIP8_SCREEN_H#define CHIP8_SCREEN_H/* Initialize the screen, allocate memory for the display. */enum INIT_STATUS Scr_init(void);/* Paint a pixel by XORing `value` onto the display at the position (`x`,`y`). Return true if a pixel was cleared   this way, which is the system's version of collision detection. */uint8_t Scr_paint(uint8_t x, uint8_t y, uint8_t value);/* Clear the display, turning every pixel off. */void Scr_clear(void);/* Print the display in 2D form to stdout, using '#' for on pixels, and ' ' for off pixels. */void Scr_print(void);#endif /* CHIP8_SCREEN_H */